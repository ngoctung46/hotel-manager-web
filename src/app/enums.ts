export enum RoomType {
  Single = 0,
  Double = 1
}

export enum RoomStatus {
  CustomerOut = 0,
  NeedCleaning = 1
}

export enum ProductType {
  RoomRate = 0,
  Service = 1,
  Item = 2,
  Payment = 3,
  ExtraCharge = 4,
}
